<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activitys.ViewMapActivity">

    <org.osmdroid.views.MapView
        android:id="@+id/openMapView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <ImageButton
        android:id="@+id/settingsButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:background="?android:selectableItemBackground"
        android:contentDescription="@string/compass"
        android:cropToPadding="true"
        android:onClick="onSettingsButtonClick"
        android:scaleType="fitXY"
        android:padding="5dip"
        app:srcCompat="@drawable/ic_settings" />

    <ImageButton
        android:id="@+id/layerButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_below="@id/settingsButton"
        android:background="?android:selectableItemBackground"
        android:contentDescription="@string/compass"
        android:cropToPadding="true"
        android:onClick="onMapLayerClick"
        android:scaleType="fitXY"
        android:padding="5dip"
        app:srcCompat="@drawable/ic_map_layer" />

    <ImageButton
        android:id="@+id/compassButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:background="?android:selectableItemBackground"
        android:contentDescription="@string/compass"
        android:cropToPadding="true"
        android:onClick="onCompassButtonClick"
        android:scaleType="fitXY"
        android:padding="5dip"
        app:srcCompat="@drawable/ic_compass_round" />

    <ImageButton
        android:id="@+id/centerButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/compassButton"
        android:layout_alignParentEnd="true"
        android:onClick="onClickButtonCenterMap"
        android:background="?android:selectableItemBackground"
        android:contentDescription="@string/center_gps"
        android:padding="5dip"
        app:srcCompat="@drawable/osm_ic_center_map" />

    <ImageButton
        android:id="@+id/createButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/centerButton"
        android:layout_alignParentEnd="true"
        android:onClick="onCreateButtonClick"
        android:background="?android:selectableItemBackground"
        android:contentDescription="@string/create_new_route"
        android:padding="5dip"
        app:srcCompat="@drawable/ic_new_topo"/>

</RelativeLayout>
