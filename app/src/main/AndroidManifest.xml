<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ar.climbing">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera2.full" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <meta-data
            android:name="com.google.ar.core"
            android:value="optional" />

        <!-- https://www.mapbox.com/api-documentation/#maps -->
        <meta-data
            android:name="MAPQUEST_MAPID"
            android:value="mapbox.streets-satellite" />
        <meta-data
            android:name="MAPQUEST_ACCESS_TOKEN"
            android:value="pk.eyJ1IjoieHl6LXJlbGF0aXZpdHkiLCJhIjoiY2poYzlkdTQ1MGFuYjJ6bG5sZWlrZm1jciJ9.KPEuGiImz7hsSA3Lht9JMA" />

        <!-- OSM keys -->
        <meta-data
            android:name="OSM_0_6_API.KEY"
            android:value="**" />
        <meta-data
            android:name="OSM_0_6_API.SECRET"
            android:value="**" />

        <!-- OSM dev keys -->
        <meta-data
            android:name="OSM_SANDBOX_0_6_API.KEY"
            android:value="**" />
        <meta-data
            android:name="OSM_SANDBOX_0_6_API.SECRET"
            android:value="**" />

        <activity
            android:name=".activitys.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activitys.ViewTopoActivity" />
        <activity android:name=".activitys.EditTopoActivity" />
        <activity android:name=".activitys.ViewMapActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="map_view"
                    android:scheme="xyzdroid" />
            </intent-filter>
        </activity>
        <activity android:name=".activitys.ToolsActivity" />
        <activity android:name=".activitys.SettingsActivity" />
        <activity android:name=".activitys.LicenseActivity" />
        <activity
            android:name=".activitys.NodesDataManagerActivity"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="data_manager"
                    android:scheme="xyzdroid" />
            </intent-filter>
        </activity>
        <activity android:name=".activitys.SupportMeActivity" />
        <activity android:name=".activitys.OAuthActivity" />
        <activity android:name=".activitys.ViewTopoArCoreActivity" />
    </application>

</manifest>